import { Component } from '@angular/core';
import { DropdownCore } from '../dropdownCore';

// Dropdown list linked to an icon button trigger

@Component({
    selector: 'dropdown-add',
    templateUrl: 'dropdownAdd.component.html',
    styleUrls: ['./dropdownAdd.component.css']
})
export class DropdownAdd extends DropdownCore {
}
